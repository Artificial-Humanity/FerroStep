[package]
name = "ferrostep-roster"
description = "The actors of a refereed loop: who they are, how they sign their work, and which document tells them how to behave"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
serde_norway = "0.9"

[dev-dependencies]
tempfile = "3"
