[package]
name = "ferrostep-ledger"
description = "The interface a FerroStep ledger adapter implements: load a record, apply a decision, enumerate, read history"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ferrostep-core = { path = "../ferrostep-core", version = "0.1.0-dev" }
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
